/// <summary>
/// All global game message types.
/// Add new messages here as the game grows.
/// </summary>
public enum GameMessageType
{
    PlayerCrashed,
    RestartRequested,

    // Future use
    PlayerJumped,
    PlayerLanded,
    ScoreChanged
}
